// saveStreak.js — reserved for on-chain streak recording via Solana.
// Will write player streaks as provable on-chain records when the
// stake pool feature is activated in a future release.
// Current player data is persisted in the server database.
